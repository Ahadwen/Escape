{
  "name": "escape",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "analyze:chess": "node scripts/analyze-chess-atlas.mjs",
    "bundle:escape": "esbuild src/escape/entry.js --bundle --format=iife --platform=browser --outfile=src/escape/standalone.js --loader:.png=dataurl",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "gh-pages": "^6.3.0",
    "pngjs": "^7.0.0",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4"
  }
}
