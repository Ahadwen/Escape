/**
 * Future: resolve R (ultimate) from equipped item / deck Ace before falling back to the character.
 * @param {object} _ctx
 * @returns {boolean} true if an item consumed the press (character should not run).
 */
export function tryUseEquippedUltimate(_ctx) {
  return false;
}
