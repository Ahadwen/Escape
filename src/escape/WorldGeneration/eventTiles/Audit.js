/**
 * Halls-only procedural **Audit** hex — forge world flow (`forgeHexFlow.js`) + `specialHexState` anchor `audit` / `auditSpent`.
 * Same interaction and rewards as the global forge tile; spawns only while the run path is Halls.
 */
