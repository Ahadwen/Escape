node_modules/
dist/
.DS_Store
.env
.env.local
.env.*.local

# gh-pages writes a nested git clone here; committing it registers a broken submodule (git exits 128 in CI).
**/node_modules/.cache/gh-pages/
